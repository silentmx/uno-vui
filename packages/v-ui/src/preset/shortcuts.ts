import type { UserShortcuts } from "unocss";
import type { Theme } from "unocss/preset-uno";

export const shortcuts: UserShortcuts<Theme> = [
  // [/^bg-(.*)$/, ([, c]) => {
  //   console.log(c);
  //   if (c == "primary") {
  //     return `bg-${selectedTheme.value.secondary}`
  //   } else {
  //     return `bg-${c}`;
  //   }
  // }],
];